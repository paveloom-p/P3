files=$(wildcard *.f90)  
obj=$(patsubst %.f90, %.o, $(files))

Prog:$(obj)
	gfortran $^ -o $@
%.o:%.f90
	gfortran -c $< -o $@
clean:
	rm -f $(obj) Prog
result:Prog
	./Prog
resplt: result Prog input
	gnuplot 'view.gnu'
