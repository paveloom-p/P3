program main
implicit none

! П: Переоформил структуру файлов.
!    Теперь можно закомментировать вызов ненужной
!    на данный момент подпрограммы.

!    Плюс, конечно, редактирование вводимых данных.

call degree

call less_than_1_binary

call simple_number

end
